/**
 * @file @Operator@Test.cpp
 * @brief Tests for the Bessel @Operator@ DenseSM operator
 */


// System includes
//
#include <catch2/catch.hpp>
#include <iostream>

// Project includes
//
#include "Types/Internal/Math.hpp"
#include "TestSuite/DenseSM/Bessel/TestArgs.hpp"
#include "DenseSM/Bessel/@Operator@.hpp"
#include "TestSuite/DenseSM/Bessel/Tester.hpp"

namespace currentts = ::QuICC::TestSuite::DenseSM::Bessel;

TEST_CASE( "Error for @Operator@ operator", "[@Operator@:@type@]" )
{

   // Set default arguments if required
   if(currentts::args().useDefault)
   {
      currentts::args().params = {0,1,2,3};
   }

   Catch::StringMaker<double>::precision = 15;

   // Set type
   currentts::args().setType("@type@");

   REQUIRE( currentts::args().params.size() > 0 );

   typedef ::QuICC::DenseSM::Bessel::@Operator@ Operator;
   typedef typename currentts::Tester<Operator> Tester;

   Tester tester("@Operator@.dat", currentts::args().dumpData);
   tester.setUlp(currentts::args().ulp);

   Tester::ParameterType params = {-1};

   for(auto id: currentts::args().params)
   {
      params.at(0) = id;
      tester.validate(params, currentts::args().type, currentts::args().timeOnly);
   
   }
}
