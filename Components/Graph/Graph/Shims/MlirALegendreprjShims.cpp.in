#include <iostream>
#include <complex>
#include <cassert>

#include "Graph/Types.hpp"
#include "Graph/Shims/MlirShims.hpp"

#ifdef QUICC_USE_PFSOLVE
#include "View/ViewSparse.hpp"
#include "Memory/Cpu/NewDelete.hpp"
#include "ViewOps/ALegendre_parallALT/Tags.hpp"
#include "ViewOps/ALegendre_parallALT/Builder.hpp"
#include <stdio.h>
#include <string.h> 
#endif

#include "ViewOps/Quadrature/Op.hpp"
#include "ViewOps/Quadrature/Impl.hpp"
#include "ViewOps/ALegendre/OpsBuilder.hpp"
#include "ViewOps/ALegendre/OpsQuadrature.hpp"
#include "Profiler/Interface.hpp"

using namespace QuICC::Graph;

/// @brief C Interface to MLIR for a al prj operator
/// column major, cpu operators
/// @param op
/// @param umod
/// @param uval
extern "C" void _ciface_quiccir_al_@Op@_@Kind@_complexf64_DCCSC3D_complexf64_S1CLCSC3D(void* obj,  view3_cd_t* pUval, view3_cd_t* pUmod)
{
    QuICC::Profiler::RegionFixture<3> fixTotal("MlirALegendreprjShims");

    #ifndef NDEBUG
    std::cout <<
        "_ciface_quiccir_al_@Op@_@Kind@_complexf64_DCCSC3D_complexf64_S1CLCSC3D\n";
    #endif
    assert(obj != nullptr);
    assert(pUval != nullptr);
    assert(pUmod != nullptr);
    // op
    using namespace QuICC::Transform::Quadrature;
    using Tin = C_S1CLCSC3D_t;
    using Tout = C_DCCSC3D_t;
    using Top = QuICC::View::View<double, QuICC::View::CS1RL3DJIK>;
    using backend_t = Cpu::ImplOp<Tout, Tin, Top,
        QuICC::Transform::Quadrature::@Treatment@>;
    using op_t = Op<Tout, Tin, Top, backend_t>;
    // views
    using namespace QuICC::View;
    constexpr std::uint32_t rank = 3;
    ViewBase<std::uint32_t> pointers[rank];
    pointers[1] = ViewBase<std::uint32_t>(pUmod->pos, pUmod->posSize);
    ViewBase<std::uint32_t> indices[rank];
    indices[1] = ViewBase<std::uint32_t>(pUmod->coo, pUmod->cooSize);
    assert(pUmod->pos == pUval->pos);
    assert(pUmod->coo == pUval->coo);
    Tin viewMod(pUmod->data, pUmod->dataSize, pUmod->dims, pointers, indices);
    Tout viewVal(pUval->data, pUval->dataSize, pUval->dims, pointers, indices);
    // Check that op was set up
    auto cl = reinterpret_cast<op_t*>(obj);
    if (cl->getOp().data() == nullptr)
    {
        QuICC::Profiler::RegionFixture<3> fixInit("MlirALegendreprjShims-init");

        /// dim 0 - Ntheta - colatitudinal points
        /// dim 1 - L  - harmonic degree
        /// dim 2 - M  - harmonic order
        std::array<std::uint32_t, rank> dims {pUval->dims[0], pUmod->dims[0], pUmod->dims[2]};
        // Distributed operator, layer might not be populated
        std::vector<std::uint32_t> layers;
        for (std::size_t i = 0; i < dims[2]; ++i) {
            if (pUmod->pos[i+1]-pUmod->pos[i] > 0)
            {
                layers.push_back(i);
            }
        }
        cl->allocOp(dims, layers);
        /// Set grid \todo set once per operator kind
        ::QuICC::Internal::Array igrid;
        ::QuICC::Internal::Array iweights;
        using namespace QuICC::Transform::ALegendre;
        OpsQuadrature<@Kind@_t> quad;
        quad.computeQuadrature(igrid, iweights, pUval->dims[0]);
        // Populate op
        auto opView = cl->getOp();
        OpsBuilder<Top, @Kind@_t, @OpDirection@> tBuilder;
        tBuilder.compute(opView, igrid, ::QuICC::Internal::Array());
    }
    assert(cl->getOp().data() != nullptr);
    // call
    cl->apply(viewVal, viewMod);
};

#ifdef QUICC_HAS_CUDA_BACKEND
#ifdef QUICC_USE_PFSOLVE
#define PfSolve_P 1
#define PfSolve_Ll 2
#define PfSolve_D1 2

//#if ((PfSolve_@Kind@ == 1) | (PfSolve_@Kind@ == 2))
/// @brief C Interface to MLIR for a al prj operator
/// column major, gpu operators
/// @param op
/// @param umod
/// @param uval
extern "C" void _ciface_quiccir_al_@Op@_@Kind@_complexf64_DCCSC3DJIK_complexf64_S1CLCSC3DJIK(void* obj,  view3_cd_t* pUval, view3_cd_t* pUmod)
{
    QuICC::Profiler::RegionFixture<3> fixTotal("MlirALegendreprjShims");

    //#ifndef NDEBUG
    std::cout <<
        "_ciface_quiccir_al_scalar_@Kind@_complexf64_DCCSC3DJIK_complexf64_S1CLCSC3DJIK\n";
    //#endif
    assert(obj != nullptr);
    assert(pUval != nullptr);
    assert(pUmod != nullptr);
    // op
    //using namespace QuICC::Transform::Quadrature;
    using Tin = C_S1CLCSC3DJIK_t;
    using Tout = C_DCCSC3DJIK_t;
    // QUICC_USE_IBLOCKED_CUDA_MATMUL_WARP_TILE
    using Top = QuICC::View::View<double, QuICC::View::CS1RL3DJIK>;
    
    // views
    using namespace QuICC::View;
    constexpr std::uint32_t rank = 3;
    ViewBase<std::uint32_t> pointers[rank];
    pointers[1] = ViewBase<std::uint32_t>(pUmod->pos, pUmod->posSize);
    ViewBase<std::uint32_t> indices[rank];
    indices[1] = ViewBase<std::uint32_t>(pUmod->coo, pUmod->cooSize);
    assert(pUmod->pos == pUval->pos);
    assert(pUmod->coo == pUval->coo);
    assert(QuICC::Cuda::isDeviceMemory(pUmod->pos));
    assert(QuICC::Cuda::isDeviceMemory(pUmod->coo));
    assert(QuICC::Cuda::isDeviceMemory(pUmod->data));
    assert(QuICC::Cuda::isDeviceMemory(pUval->data));
    Tin viewMod(pUmod->data, pUmod->dataSize, pUmod->dims, pointers, indices);
    Tout viewVal(pUval->data, pUval->dataSize, pUval->dims, pointers, indices);
    // Check that op was set up
    //auto cl = reinterpret_cast<op_t*>(obj);
    using mods_t = QuICC::View::View<std::complex<double>, QuICC::View::S1CLCSC3DJIK>;
    using phys_t = QuICC::View::View<std::complex<double>, QuICC::View::DCCSC3DJIK>;
   
    QuICC::Transform::ALegendre_parallALT::ParallaltOp<phys_t, mods_t>* cl = (QuICC::Transform::ALegendre_parallALT::ParallaltOp<phys_t, mods_t>*)obj;
    
    const char* proj_operator = "@Kind@";
    printf("ALT %s\n", proj_operator);
    if (!strcmp(proj_operator, "P")){
        cl->setType(0, 1);
        cl->apply(viewVal, viewMod);
    }
    if (!strcmp(proj_operator, "Ll")){
        cl->setType(1, 1);
        cl->apply(viewVal, viewMod);
    }
};
//#else

extern "C" void _ciface_quiccir_al_@Op@V_@Kind@_complexf64_DCCSC3DJIK_complexf64_DCCSC3DJIK_complexf64_S1CLCSC3DJIK_complexf64_S1CLCSC3DJIK(void* obj,  view3_cd_t* pUval, view3_cd_t* pUval2, view3_cd_t* pUmod,  view3_cd_t* pUmod2)
{
    QuICC::Profiler::RegionFixture<3> fixTotal("MlirALegendreprjShims");

    //#ifndef NDEBUG
    std::cout <<
        "_ciface_quiccir_al_scalar_@Kind@V_complexf64_DCCSC3DJIK_complexf64_DCCSC3DJIK_complexf64_S1CLCSC3DJIK_complexf64_S1CLCSC3DJIK\n";
    //#endif
    assert(obj != nullptr);
    assert(pUval != nullptr);
    assert(pUmod != nullptr);
    // op
    //using namespace QuICC::Transform::Quadrature;
    using Tin = C_S1CLCSC3DJIK_t;
    using Tout = C_DCCSC3DJIK_t;
    // QUICC_USE_IBLOCKED_CUDA_MATMUL_WARP_TILE
    using Top = QuICC::View::View<double, QuICC::View::CS1RL3DJIK>;
    
    // views
    using namespace QuICC::View;
    constexpr std::uint32_t rank = 3;
    ViewBase<std::uint32_t> pointers[rank];
    pointers[1] = ViewBase<std::uint32_t>(pUmod->pos, pUmod->posSize);
    ViewBase<std::uint32_t> indices[rank];
    indices[1] = ViewBase<std::uint32_t>(pUmod->coo, pUmod->cooSize);
    assert(pUmod->pos == pUval->pos);
    assert(pUmod->coo == pUval->coo);
    assert(QuICC::Cuda::isDeviceMemory(pUmod->pos));
    assert(QuICC::Cuda::isDeviceMemory(pUmod->coo));
    assert(QuICC::Cuda::isDeviceMemory(pUmod->data));
    assert(QuICC::Cuda::isDeviceMemory(pUval->data));
    Tin viewMod(pUmod->data, pUmod->dataSize, pUmod->dims, pointers, indices);
    Tout viewVal(pUval->data, pUval->dataSize, pUval->dims, pointers, indices);
    Tin viewMod2(pUmod2->data, pUmod2->dataSize, pUmod2->dims, pointers, indices);
    Tout viewVal2(pUval2->data, pUval2->dataSize, pUval2->dims, pointers, indices);
    // Check that op was set up
    //auto cl = reinterpret_cast<op_t*>(obj);
    using mods_t = QuICC::View::View<std::complex<double>, QuICC::View::S1CLCSC3DJIK>;
    using phys_t = QuICC::View::View<std::complex<double>, QuICC::View::DCCSC3DJIK>;
   
    QuICC::Transform::ALegendre_parallALT::ParallaltOp<phys_t, mods_t>* cl = (QuICC::Transform::ALegendre_parallALT::ParallaltOp<phys_t, mods_t>*)obj;
    
    const char* proj_operator = "@Kind@";
    printf("ALT %s\n", proj_operator);
    if (!strcmp(proj_operator, "P")){
        cl->setType(0, 1);
        cl->apply(viewVal, viewMod);
    }
    if (!strcmp(proj_operator, "Ll")){
        cl->setType(1, 1);
        cl->apply(viewVal, viewMod);
    }
    if (!strcmp(proj_operator, "D1")){
        cl->setType(10, 1);
        // call
        cl->apply(viewVal, viewVal2, viewMod, viewMod2);
    }
    if (!strcmp(proj_operator, "DivS1")){
        cl->setType(20, 1);
        // call
        cl->apply(viewVal, viewVal2, viewMod, viewMod2);
    }
    if (!strcmp(proj_operator, "LlD1")){
        cl->setType(110, 1);
        cl->apply(viewVal, viewVal2, viewMod, viewMod2);
    }
};
//#endif
/// @brief C Interface to MLIR for a al prj operator
/// column major, gpu operators
/// @param op
/// @param umod
/// @param uval
/*extern "C" void _ciface_quiccir_al_vector_@Kind@_complexf64_DCCSC3DJIK_complexf64_S1CLCSC3DJIK(void* obj,  view3_cd_t* pUval_s, view3_cd_t* pUval_t, view3_cd_t* pUmod_s, view3_cd_t* pUmod_t)
{
    QuICC::Profiler::RegionFixture<3> fixTotal("MlirALegendreprjShims");

    #ifndef NDEBUG
    std::cout <<
        "_ciface_quiccir_al_vector_@Kind@_complexf64_DCCSC3DJIK_complexf64_S1CLCSC3DJIK\n";
    #endif
    assert(obj != nullptr);
    assert(pUval_s != nullptr);
    assert(pUmod_s != nullptr);
    assert(pUval_t != nullptr);
    assert(pUmod_t != nullptr);
    // op
    using namespace QuICC::Transform::Quadrature;
    using Tin = C_S1CLCSC3DJIK_t;
    using Tout = C_DCCSC3DJIK_t;
    // QUICC_USE_IBLOCKED_CUDA_MATMUL_WARP_TILE
    using Top = QuICC::View::View<double, QuICC::View::CS1RL3DJIK>;
    
    // views
    using namespace QuICC::View;
    constexpr std::uint32_t rank = 3;
    ViewBase<std::uint32_t> pointers[rank];
    pointers[1] = ViewBase<std::uint32_t>(pUmod_s->pos, pUmod_s->posSize);
    ViewBase<std::uint32_t> indices[rank];
    indices[1] = ViewBase<std::uint32_t>(pUmod_s->coo, pUmod_s->cooSize);

    assert(pUmod_s->pos == pUval_s->pos);
    assert(pUmod_s->coo == pUval_s->coo);
    assert(QuICC::Cuda::isDeviceMemory(pUmod_s->pos));
    assert(QuICC::Cuda::isDeviceMemory(pUmod_s->coo));
    assert(QuICC::Cuda::isDeviceMemory(pUmod_s->data));
    assert(QuICC::Cuda::isDeviceMemory(pUval_s->data));

    assert(pUmod_t->pos == pUval_t->pos);
    assert(pUmod_t->coo == pUval_t->coo);
    assert(QuICC::Cuda::isDeviceMemory(pUmod_t->pos));
    assert(QuICC::Cuda::isDeviceMemory(pUmod_t->coo));
    assert(QuICC::Cuda::isDeviceMemory(pUmod_t->data));
    assert(QuICC::Cuda::isDeviceMemory(pUval_t->data));

    Tin viewMod_s(pUmod_s->data, pUmod_s->dataSize, pUmod_s->dims, pointers, indices);
    Tout viewVal_s(pUval_s->data, pUval_s->dataSize, pUval_s->dims, pointers, indices);
    Tin viewMod_t(pUmod_t->data, pUmod_t->dataSize, pUmod_t->dims, pointers, indices);
    Tout viewVal_t(pUval_t->data, pUval_t->dataSize, pUval_t->dims, pointers, indices);
    // Check that op was set up
    //auto cl = reinterpret_cast<op_t*>(obj);
    using mods_t = QuICC::View::View<std::complex<double>, QuICC::View::S1CLCSC3DJIK>;
    using phys_t = QuICC::View::View<std::complex<double>, QuICC::View::DCCSC3DJIK>;
   
    QuICC::Transform::ALegendre_parallALT::ParallaltOp<phys_t, mods_t>* cl = (QuICC::Transform::ALegendre_parallALT::ParallaltOp<phys_t, mods_t>*)obj;
    
    cl->setType(1, 1);
    // call
    cl->apply(viewVal_s, viewVal_t, viewMod_s, viewMod_t);
};*/
#else
/// @brief C Interface to MLIR for a al prj operator
/// column major, gpu operators
/// @param op
/// @param umod
/// @param uval
extern "C" void _ciface_quiccir_al_@Op@_@Kind@_complexf64_DCCSC3DJIK_complexf64_S1CLCSC3DJIK(void* obj,  view3_cd_t* pUval, view3_cd_t* pUmod)
{
    QuICC::Profiler::RegionFixture<3> fixTotal("MlirALegendreprjShims");

    #ifndef NDEBUG
    std::cout <<
        "_ciface_quiccir_al_@Op@_@Kind@_complexf64_DCCSC3DJIK_complexf64_S1CLCSC3DJIK\n";
    #endif
    assert(obj != nullptr);
    assert(pUval != nullptr);
    assert(pUmod != nullptr);
    // op
    using namespace QuICC::Transform::Quadrature;
    using Tin = C_S1CLCSC3DJIK_t;
    using Tout = C_DCCSC3DJIK_t;
    // QUICC_USE_IBLOCKED_CUDA_MATMUL_WARP_TILE
    using Top = QuICC::View::View<double, QuICC::View::CS1RL3DJIK>;
    using backend_t = Cuda::ImplOp<Tout, Tin, Top,
        QuICC::Transform::Quadrature::@Treatment@>;
    using op_t = Op<Tout, Tin, Top, backend_t>;
    // views
    using namespace QuICC::View;
    constexpr std::uint32_t rank = 3;
    ViewBase<std::uint32_t> pointers[rank];
    pointers[1] = ViewBase<std::uint32_t>(pUmod->pos, pUmod->posSize);
    ViewBase<std::uint32_t> indices[rank];
    indices[1] = ViewBase<std::uint32_t>(pUmod->coo, pUmod->cooSize);
    assert(pUmod->pos == pUval->pos);
    assert(pUmod->coo == pUval->coo);
    assert(QuICC::Cuda::isDeviceMemory(pUmod->pos));
    assert(QuICC::Cuda::isDeviceMemory(pUmod->coo));
    assert(QuICC::Cuda::isDeviceMemory(pUmod->data));
    assert(QuICC::Cuda::isDeviceMemory(pUval->data));
    Tin viewMod(pUmod->data, pUmod->dataSize, pUmod->dims, pointers, indices);
    Tout viewVal(pUval->data, pUval->dataSize, pUval->dims, pointers, indices);
    // Check that op was set up
    auto cl = reinterpret_cast<op_t*>(obj);
    if (cl->getOp().data() == nullptr)
    {
        QuICC::Profiler::RegionFixture<3> fixInit("MlirALegendreprjShims-init");

        /// dim 0 - Ntheta - colatitudinal points
        /// dim 1 - L  - harmonic degree
        /// dim 2 - M  - harmonic order
        std::array<std::uint32_t, rank> dims {pUval->dims[0], pUmod->dims[0], pUmod->dims[2]};
        // Distributed operator, layer might not be populated
        std::vector<std::uint32_t> layers;
        // Converter for metadata
        auto pos = pointers[1];
        using namespace QuICC::Memory;
        tempOnHostMemorySpace converterP(pos, TransferMode::read | TransferMode::block);
        for (std::size_t i = 0; i < dims[2]; ++i) {
            if (pos[i+1]-pos[i] > 0)
            {
                layers.push_back(i);
            }
        }
        cl->allocOp(dims, layers);
        /// Set grid \todo set once per operator kind
        ::QuICC::Internal::Array igrid;
        ::QuICC::Internal::Array iweights;
        using namespace QuICC::Transform::ALegendre;
        OpsQuadrature<@Kind@_t> quad;
        quad.computeQuadrature(igrid, iweights, pUval->dims[0]);
        // Populate op
        auto opView = cl->getOp();
        OpsBuilder<Top, @Kind@_t, @OpDirection@> tBuilder;
        tBuilder.compute(opView, igrid, ::QuICC::Internal::Array());
    }
    assert(cl->getOp().data() != nullptr);
    // call
    cl->apply(viewVal, viewMod);
};

#endif
#endif
