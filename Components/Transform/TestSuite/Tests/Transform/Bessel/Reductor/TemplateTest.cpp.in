/**
 * @file @Operator@Test.cpp
 * @brief Tests for the Bessel based @Operator@ reductor
 */


// Configuration includes
//

// System includes
//
#include <catch2/catch.hpp>

// Project includes
//
#include "QuICC/TestSuite/Transform/Bessel/TestArgs.hpp"
#include "QuICC/TestSuite/Transform/Bessel/Tester.hpp"
#include "QuICC/Transform/Poly/Bessel/Reductor/Base/@Boundary@.hpp"
#include "QuICC/Transform/Poly/Bessel/Reductor/@Operator@.hpp"

namespace currentts = ::QuICC::TestSuite::Transform::Bessel;

TEST_CASE( "Error for @Boundary@@Operator@@Template@ reductor", "[@CatchTestName@]" ){

   // Set default arguments if required
   if(currentts::args().useDefault)
   {
      currentts::args().params = {0,1,2};
   }

   Catch::StringMaker<double>::precision = 15;

   // Set type
   currentts::args().setType("@type@");

   REQUIRE( (currentts::args().type == ::QuICC::TestSuite::Transform::TestType::REDUCTOR && "Test type is reductor") );
   REQUIRE( currentts::args().params.size() > 0 );

   // Typedef for used operator
   using namespace ::QuICC::Transform::Poly::Bessel;
   typedef Reductor::@Boundary@<Reductor::@Operator@@Template@> Reductor;
   typedef typename currentts::Tester<Reductor> Tester;

   Tester tester("@Boundary@", "@Operator@.dat", currentts::args().dumpData);
   tester.setUlp(currentts::args().ulp);
   tester.setIter(currentts::args().iter);

   Tester::ParameterType params = {-1};

   if(currentts::args().np > 0)
   {
      params.push_back(currentts::args().np);
      params.push_back(currentts::args().rank);
   }

   for(int id: currentts::args().params)
   {
      params.at(0) = id;
      tester.validate(params, currentts::args().type, currentts::args().timeOnly);
   }

}
